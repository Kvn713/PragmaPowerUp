server:
  port: 8080
spring:
  application:
    name: "CrediYa_Ms_Autenticacion"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
adapters:
  r2dbc:
    host: localhost
    port: 3306
    database: Autenticacion
    username: root
    password: P@55w0rd
spring-doc:
  swagger-ui:
    path: /swagger-ui.html
logging:
  level:
    root: INFO
    co.com.pragma.crediya: TRACE
